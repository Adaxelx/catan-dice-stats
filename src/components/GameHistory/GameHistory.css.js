import styled from "styled-components";

export const StyledStat = styled.div`
  display: flex;
  align-items: center;
`;

export const StyledDiceStat = styled.span`
  white-space: nowrap;
  flex-grow: 1;
`;
